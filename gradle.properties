## For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
#
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx1024m -XX:MaxPermSize=256m
# org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
#
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
#Sun Feb 21 19:40:15 PST 2021
android.enableJetifier=true
android.useAndroidX=true
# Use classic Kotlin Gradle plugin + kapt (Hilt/Room) alongside AGP until full built-in Kotlin migration.
android.builtInKotlin=false
android.newDsl=false
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2048M -Dkotlin.daemon.jvm.options=-Xmx2048M


MYAPP_UPLOAD_STORE_FILE=CarditekKeystore1.jks

MYAPP_UPLOAD_KEY_ALIAS =Carditek-key

MYAPP_UPLOAD_STORE_PASSWORD=Carditek@2022

MYAPP_UPLOAD_KEY_PASSWORD=Carditek@2022

# Telegram: set BOT_TOKEN + CHAT_ID (optional; use local ~/.gradle/gradle.properties for secrets).
# Omit TELEGRAM_ENABLED to auto-enable when both values are non-empty, or set TELEGRAM_ENABLED=false to force off.
# TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN= 7597526068:AAGVJwkXbUO3R93UH4yWHtW5En-pYDf9Dl8
TELEGRAM_CHAT_ID= 738070910

